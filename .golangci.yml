version: "2"

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
