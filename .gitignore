.env
*.env
!.env.example

# Бинарь от `go build ./cmd/parser` в корне. Слэш обязателен: без него паттерн
# матчил бы и каталог cmd/parser/ — из-за этого его исходники не попадали в git.
/parser
/bin/
/out/

# Выгрузки экспортёра: артефакты прогона, не исходники.
*.xlsx

# Ключ сервис-аккаунта Google (stretch-goal) — секрет, не должен попасть в репозиторий.
service-account.json
credentials.json

*.exe
*.test
*.out
coverage.*

.DS_Store
.idea/
.vscode/
